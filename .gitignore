# dependencies
/node_modules

# production
/dist
/.next

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo

# editor & local tools
/.claude/

# private internal documentation & planning notes
/docs/
CONTENT_GUIDE.md
COPY_REVIEW.md
ROADMAP.md

# design source reference (raw MCP pulls, not app code)
/design-reference/

# supplied image originals — source material, like ProjectsDokus/, which lives
# outside the repo for the same reason. Only finished exports in public/images
# ship; docs/reference/image_crops.json records the path each one came from.
/Images/

# The project documentations — 644 MB of PDFs. These have always been source
# material that must not ship (DECISION-016, image_sources.md); they used to
# live outside the repository, and a copy has since appeared inside it. Ignored
# so that `git add -A` cannot commit them by accident.
/ProjectsDokus/
