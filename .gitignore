# dependencies
/node_modules

# production
/dist
/.next

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo

# design source reference (raw MCP pulls, not app code)
/design-reference/

# supplied image originals — source material, like ProjectsDokus/, which lives
# outside the repo for the same reason. Only finished exports in public/images
# ship; docs/reference/image_crops.json records the path each one came from.
/Images/
